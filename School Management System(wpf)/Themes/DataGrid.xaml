<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Soft modern table system used app-wide -->
    <SolidColorBrush x:Key="DataGrid.HeaderBackground" Color="#EEF4FB"/>
    <SolidColorBrush x:Key="DataGrid.HeaderHover" Color="#E2EDF8"/>
    <SolidColorBrush x:Key="DataGrid.HeaderText" Color="#1E3A5F"/>
    <SolidColorBrush x:Key="DataGrid.Border" Color="#D0DBE8"/>
    <SolidColorBrush x:Key="DataGrid.RowAlt" Color="#F7FBFE"/>
    <SolidColorBrush x:Key="DataGrid.RowHover" Color="#F0F7FF"/>
    <SolidColorBrush x:Key="DataGrid.RowSelected" Color="#E4F0FC"/>
    <SolidColorBrush x:Key="DataGrid.RowSelectedHover" Color="#D7EAFB"/>
    <SolidColorBrush x:Key="DataGrid.GridLine" Color="#E9EFF6"/>
    <SolidColorBrush x:Key="DataGrid.Accent" Color="#0F6CBD"/>

    <Style TargetType="DataGrid">
        <Setter Property="Margin" Value="0"/>
        <Setter Property="AutoGenerateColumns" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGrid.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
        <Setter Property="RowBackground" Value="#FFFFFF"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource DataGrid.RowAlt}"/>
        <Setter Property="RowHeight" Value="38"/>
        <Setter Property="ColumnHeaderHeight" Value="42"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource DataGrid.GridLine}"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="ClipToBounds" Value="True"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <!-- Slight inset so tables feel framed, not flush -->
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource DataGrid.HeaderBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource DataGrid.HeaderText}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="BorderBrush" Value="#D8E3EF"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="Padding" Value="14,0"/>
        <Setter Property="Height" Value="42"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"/>
                                <!-- Sort cue -->
                                <Path x:Name="SortArrow"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Margin="0,0,6,0"
                                      Width="8"
                                      Height="5"
                                      Stretch="Fill"
                                      Opacity="0"
                                      Fill="{StaticResource DataGrid.HeaderText}"
                                      Data="M0,5 L4,0 L8,5 Z"/>
                            </Grid>
                        </Border>
                        <!-- Bottom accent rule under headers -->
                        <Border Height="2"
                                VerticalAlignment="Bottom"
                                Background="#C5D8EE"
                                Opacity="0.9"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource DataGrid.HeaderHover}"/>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortArrow" Property="Opacity" Value="0.85"/>
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource DataGrid.HeaderHover}"/>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortArrow" Property="Opacity" Value="0.85"/>
                            <Setter TargetName="SortArrow" Property="Data" Value="M0,0 L4,5 L8,0 Z"/>
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource DataGrid.HeaderHover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="3,0,0,0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="MinHeight" Value="38"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGrid.RowHover}"/>
                <Setter Property="BorderBrush" Value="#B7D0EC"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGrid.RowSelected}"/>
                <Setter Property="BorderBrush" Value="{StaticResource DataGrid.Accent}"/>
                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource DataGrid.RowSelectedHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.PrimaryHover}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Padding" Value="14,6"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Optional named style for explicit use -->
    <Style x:Key="ModernDataGrid" TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}"/>
</ResourceDictionary>
