<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
      Clean button system:
      - Compact, consistent height (not stretched by parent layout)
      - Hover colors live on Style.Triggers so Ghost/Secondary/Danger keep their own palettes
      - Shared rounded chrome via template
    -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            CornerRadius="8"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Brush.Focus}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.PrimaryHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.PrimaryHover}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.PrimaryPressed}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.PrimaryPressed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Neutral / secondary surfaces -->
    <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="Foreground" Value="#334155"/>
        <Setter Property="BorderBrush" Value="#CBD5E1"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F8FAFC"/>
                <Setter Property="BorderBrush" Value="#94A3B8"/>
                <Setter Property="Foreground" Value="#0F172A"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#F1F5F9"/>
                <Setter Property="BorderBrush" Value="#64748B"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SecondaryActionButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#EFF6FF"/>
        <Setter Property="BorderBrush" Value="#93C5FD"/>
        <Setter Property="Foreground" Value="#1D4ED8"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#DBEAFE"/>
                <Setter Property="BorderBrush" Value="#60A5FA"/>
                <Setter Property="Foreground" Value="#1E40AF"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#2563EB"/>
                <Setter Property="BorderBrush" Value="#2563EB"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PageActionButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#2563EB"/>
        <Setter Property="BorderBrush" Value="#2563EB"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="14,5"/>
        <Setter Property="MinWidth" Value="96"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1D4ED8"/>
                <Setter Property="BorderBrush" Value="#1D4ED8"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#1E40AF"/>
                <Setter Property="BorderBrush" Value="#1E40AF"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DangerGhostButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#FEF2F2"/>
        <Setter Property="BorderBrush" Value="#FCA5A5"/>
        <Setter Property="Foreground" Value="#B91C1C"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FEE2E2"/>
                <Setter Property="BorderBrush" Value="#F87171"/>
                <Setter Property="Foreground" Value="#991B1B"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#DC2626"/>
                <Setter Property="BorderBrush" Value="#DC2626"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Semantic toolbar actions -->
    <Style x:Key="EditActionButton" TargetType="Button" BasedOn="{StaticResource SecondaryActionButton}">
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="Padding" Value="12,5"/>
    </Style>

    <Style x:Key="DeleteActionButton" TargetType="Button" BasedOn="{StaticResource DangerGhostButton}">
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="Padding" Value="12,5"/>
    </Style>

    <Style x:Key="ManageActionButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#ECFEFF"/>
        <Setter Property="BorderBrush" Value="#67E8F9"/>
        <Setter Property="Foreground" Value="#0E7490"/>
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="Padding" Value="12,5"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#CFFAFE"/>
                <Setter Property="BorderBrush" Value="#22D3EE"/>
                <Setter Property="Foreground" Value="#155E75"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#0891B2"/>
                <Setter Property="BorderBrush" Value="#0891B2"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RefreshActionButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="MinWidth" Value="72"/>
    </Style>

    <Style x:Key="CancelActionButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="MinWidth" Value="72"/>
    </Style>

    <Style x:Key="ArchiveActionButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#FFFBEB"/>
        <Setter Property="BorderBrush" Value="#FCD34D"/>
        <Setter Property="Foreground" Value="#B45309"/>
        <Setter Property="MinWidth" Value="88"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FEF3C7"/>
                <Setter Property="BorderBrush" Value="#FBBF24"/>
                <Setter Property="Foreground" Value="#92400E"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D97706"/>
                <Setter Property="BorderBrush" Value="#D97706"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SaveActionButton" TargetType="Button" BasedOn="{StaticResource PageActionButton}">
        <Setter Property="MinWidth" Value="80"/>
    </Style>

    <Style x:Key="NewActionButton" TargetType="Button" BasedOn="{StaticResource PageActionButton}">
        <Setter Property="MinWidth" Value="88"/>
    </Style>

    <Style x:Key="TopBarGhostButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="TopBarPrimaryButton" TargetType="Button" BasedOn="{StaticResource PageActionButton}">
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="TopBarDangerButton" TargetType="Button" BasedOn="{StaticResource DangerGhostButton}">
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SideNavButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Background" Value="{StaticResource Brush.NavButton}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.NavButtonBorder}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.NavButtonText}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.NavButtonHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.NavButtonActiveBorder}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#163958"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SideNavMajorButton" TargetType="Button" BasedOn="{StaticResource SideNavButton}">
        <Setter Property="Background" Value="#142F4B"/>
        <Setter Property="BorderBrush" Value="#2C4F74"/>
        <Setter Property="Foreground" Value="#D2E5FA"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="12,7"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1D3E61"/>
                <Setter Property="BorderBrush" Value="#3B6794"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#17344F"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SideNavSubButton" TargetType="Button" BasedOn="{StaticResource SideNavButton}">
        <Setter Property="Background" Value="#12283F"/>
        <Setter Property="BorderBrush" Value="#224668"/>
        <Setter Property="Foreground" Value="#C8DCF4"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="16,6"/>
        <Setter Property="Margin" Value="10,0,0,5"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1A3858"/>
                <Setter Property="BorderBrush" Value="#3A678F"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#152F4A"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LinkButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="MinHeight" Value="24"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#ECF2FF"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource Brush.PrimaryHover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="OpsModuleButton" TargetType="Button" BasedOn="{StaticResource SecondaryActionButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="12,9"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="#D5DEE9"/>
        <Setter Property="Foreground" Value="#1E3A5F"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#EFF6FF"/>
                <Setter Property="BorderBrush" Value="#93C5FD"/>
                <Setter Property="Foreground" Value="#1D4ED8"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#DBEAFE"/>
                <Setter Property="BorderBrush" Value="#60A5FA"/>
                <Setter Property="Foreground" Value="#1E40AF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WorkbenchPrimaryActionButton" TargetType="Button" BasedOn="{StaticResource PageActionButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

    <Style x:Key="WorkbenchSecondaryActionButton" TargetType="Button" BasedOn="{StaticResource SecondaryActionButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

    <Style x:Key="WorkbenchDangerActionButton" TargetType="Button" BasedOn="{StaticResource DangerGhostButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

    <Style x:Key="DashboardHubTileButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="#B8CCE4"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="MinHeight" Value="132"/>
        <Setter Property="Margin" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#EAF5FF"/>
                <Setter Property="BorderBrush" Value="#5BB7E8"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D7EEFF"/>
                <Setter Property="BorderBrush" Value="#369ED8"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
