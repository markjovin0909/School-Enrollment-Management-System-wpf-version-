<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="Legacy.BgColor">#F4F7FB</Color>
    <Color x:Key="Legacy.PanelColor">#FFFFFF</Color>
    <Color x:Key="Legacy.PanelAltColor">#EEF3F9</Color>
    <Color x:Key="Legacy.BorderColor">#D7DFEA</Color>
    <Color x:Key="Legacy.BorderStrongColor">#C4D0DE</Color>
    <Color x:Key="Legacy.HeaderColor">#172554</Color>
    <Color x:Key="Legacy.AccentColor">#2563EB</Color>
    <Color x:Key="Legacy.AccentHoverColor">#1D4ED8</Color>
    <Color x:Key="Legacy.TextColor">#0F172A</Color>
    <Color x:Key="Legacy.TextMutedColor">#64748B</Color>
    <Color x:Key="Legacy.WarningColor">#D97706</Color>
    <Color x:Key="Legacy.DangerColor">#DC2626</Color>
    <Color x:Key="Legacy.SuccessColor">#059669</Color>

    <SolidColorBrush x:Key="Legacy.BgBrush" Color="{StaticResource Legacy.BgColor}"/>
    <SolidColorBrush x:Key="Legacy.PanelBrush" Color="{StaticResource Legacy.PanelColor}"/>
    <SolidColorBrush x:Key="Legacy.PanelAltBrush" Color="{StaticResource Legacy.PanelAltColor}"/>
    <SolidColorBrush x:Key="Legacy.BorderBrush" Color="{StaticResource Legacy.BorderColor}"/>
    <SolidColorBrush x:Key="Legacy.BorderStrongBrush" Color="{StaticResource Legacy.BorderStrongColor}"/>
    <SolidColorBrush x:Key="Legacy.HeaderBrush" Color="{StaticResource Legacy.HeaderColor}"/>
    <SolidColorBrush x:Key="Legacy.AccentBrush" Color="{StaticResource Legacy.AccentColor}"/>
    <SolidColorBrush x:Key="Legacy.AccentHoverBrush" Color="{StaticResource Legacy.AccentHoverColor}"/>
    <SolidColorBrush x:Key="Legacy.TextBrush" Color="{StaticResource Legacy.TextColor}"/>
    <SolidColorBrush x:Key="Legacy.TextMutedBrush" Color="{StaticResource Legacy.TextMutedColor}"/>
    <SolidColorBrush x:Key="Legacy.WarningBrush" Color="{StaticResource Legacy.WarningColor}"/>
    <SolidColorBrush x:Key="Legacy.DangerBrush" Color="{StaticResource Legacy.DangerColor}"/>
    <SolidColorBrush x:Key="Legacy.SuccessBrush" Color="{StaticResource Legacy.SuccessColor}"/>

    <SolidColorBrush x:Key="Brush.Background" Color="{StaticResource Legacy.BgColor}"/>
    <SolidColorBrush x:Key="Brush.Surface" Color="{StaticResource Legacy.PanelColor}"/>
    <SolidColorBrush x:Key="Brush.SurfaceAlt" Color="{StaticResource Legacy.PanelAltColor}"/>
    <SolidColorBrush x:Key="Brush.Border" Color="{StaticResource Legacy.BorderColor}"/>
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource Legacy.BorderColor}"/>
    <SolidColorBrush x:Key="Brush.BorderStrong" Color="{StaticResource Legacy.BorderStrongColor}"/>
    <SolidColorBrush x:Key="Brush.TextPrimary" Color="{StaticResource Legacy.TextColor}"/>
    <SolidColorBrush x:Key="Brush.TextSecondary" Color="{StaticResource Legacy.TextMutedColor}"/>
    <SolidColorBrush x:Key="Brush.Info" Color="{StaticResource Legacy.AccentColor}"/>
    <SolidColorBrush x:Key="Brush.Warning" Color="{StaticResource Legacy.WarningColor}"/>
    <SolidColorBrush x:Key="Brush.Danger" Color="{StaticResource Legacy.DangerColor}"/>
    <SolidColorBrush x:Key="Brush.Success" Color="{StaticResource Legacy.SuccessColor}"/>
    <SolidColorBrush x:Key="BrushMuted" Color="{StaticResource Legacy.TextMutedColor}"/>

    <DropShadowEffect x:Key="ModernPanelShadow"
                      BlurRadius="18"
                      Direction="270"
                      ShadowDepth="2"
                      Opacity="0.09"
                      Color="#0F172A"/>

    <Style x:Key="Text.PageTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
    </Style>

    <Style x:Key="Text.SectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="17"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.HeaderBrush}"/>
    </Style>

    <Style x:Key="Text.Subtitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="LineHeight" Value="18"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextMutedBrush}"/>
    </Style>

    <Style x:Key="Text.Caption" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextMutedBrush}"/>
    </Style>

    <Style x:Key="Text.CaptionStrong" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.HeaderBrush}"/>
    </Style>

    <Style x:Key="Text.Body" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
    </Style>

    <Style x:Key="LegacyMenu" TargetType="Menu">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
        <Setter Property="Padding" Value="2,0"/>
    </Style>

    <Style x:Key="LegacyMenuItem" TargetType="MenuItem">
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Margin" Value="0,0,2,0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
    </Style>

    <Style x:Key="LegacyToolBar" TargetType="ToolBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style x:Key="LegacyToolBarButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#E8F0FF"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="#CFE0FF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#DCE8FF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LegacyButton" TargetType="Button">
        <Setter Property="Foreground" Value="#334155"/>
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="#CBD5E1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F8FAFC"/>
                <Setter Property="BorderBrush" Value="#94A3B8"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#F1F5F9"/>
                <Setter Property="BorderBrush" Value="#64748B"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LegacyPrimaryButton" TargetType="Button" BasedOn="{StaticResource LegacyButton}">
        <Setter Property="Background" Value="{StaticResource Legacy.AccentBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.AccentBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Legacy.AccentHoverBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Legacy.AccentHoverBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#1E40AF"/>
                <Setter Property="BorderBrush" Value="#1E40AF"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LegacyDangerButton" TargetType="Button" BasedOn="{StaticResource LegacyButton}">
        <Setter Property="Background" Value="#FEF2F2"/>
        <Setter Property="BorderBrush" Value="#FCA5A5"/>
        <Setter Property="Foreground" Value="#B91C1C"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FEE2E2"/>
                <Setter Property="BorderBrush" Value="#F87171"/>
                <Setter Property="Foreground" Value="#991B1B"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#DC2626"/>
                <Setter Property="BorderBrush" Value="#DC2626"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource LegacyPrimaryButton}"/>
    <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource LegacyButton}"/>
    <Style x:Key="DangerGhostButton" TargetType="Button" BasedOn="{StaticResource LegacyDangerButton}"/>
    <Style x:Key="SecondaryActionButton" TargetType="Button" BasedOn="{StaticResource LegacyButton}">
        <Setter Property="Background" Value="#EFF6FF"/>
        <Setter Property="BorderBrush" Value="#93C5FD"/>
        <Setter Property="Foreground" Value="#1D4ED8"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#DBEAFE"/>
                <Setter Property="BorderBrush" Value="#60A5FA"/>
                <Setter Property="Foreground" Value="#1E40AF"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#2563EB"/>
                <Setter Property="BorderBrush" Value="#2563EB"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="PageActionButton" TargetType="Button" BasedOn="{StaticResource LegacyPrimaryButton}"/>
    <Style x:Key="EditActionButton" TargetType="Button" BasedOn="{StaticResource SecondaryActionButton}"/>
    <Style x:Key="DeleteActionButton" TargetType="Button" BasedOn="{StaticResource DangerGhostButton}"/>
    <Style x:Key="ManageActionButton" TargetType="Button" BasedOn="{StaticResource LegacyButton}">
        <Setter Property="Background" Value="#ECFEFF"/>
        <Setter Property="BorderBrush" Value="#67E8F9"/>
        <Setter Property="Foreground" Value="#0E7490"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#CFFAFE"/>
                <Setter Property="BorderBrush" Value="#22D3EE"/>
                <Setter Property="Foreground" Value="#155E75"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#0891B2"/>
                <Setter Property="BorderBrush" Value="#0891B2"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="RefreshActionButton" TargetType="Button" BasedOn="{StaticResource GhostButton}"/>
    <Style x:Key="CancelActionButton" TargetType="Button" BasedOn="{StaticResource GhostButton}"/>
    <Style x:Key="SaveActionButton" TargetType="Button" BasedOn="{StaticResource LegacyPrimaryButton}"/>
    <Style x:Key="NewActionButton" TargetType="Button" BasedOn="{StaticResource LegacyPrimaryButton}"/>
    <Style x:Key="ArchiveActionButton" TargetType="Button" BasedOn="{StaticResource LegacyButton}">
        <Setter Property="Background" Value="#FFFBEB"/>
        <Setter Property="BorderBrush" Value="#FCD34D"/>
        <Setter Property="Foreground" Value="#B45309"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FEF3C7"/>
                <Setter Property="BorderBrush" Value="#FBBF24"/>
                <Setter Property="Foreground" Value="#92400E"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D97706"/>
                <Setter Property="BorderBrush" Value="#D97706"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="WorkbenchPrimaryActionButton" TargetType="Button" BasedOn="{StaticResource LegacyPrimaryButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>
    <Style x:Key="WorkbenchSecondaryActionButton" TargetType="Button" BasedOn="{StaticResource SecondaryActionButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>
    <Style x:Key="WorkbenchDangerActionButton" TargetType="Button" BasedOn="{StaticResource DangerGhostButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

    <Style x:Key="OpsModuleButton" TargetType="Button" BasedOn="{StaticResource SecondaryActionButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="12,9"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="#D5DEE9"/>
        <Setter Property="Foreground" Value="#1E3A5F"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#EFF6FF"/>
                <Setter Property="BorderBrush" Value="#93C5FD"/>
                <Setter Property="Foreground" Value="#1D4ED8"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#DBEAFE"/>
                <Setter Property="BorderBrush" Value="#60A5FA"/>
                <Setter Property="Foreground" Value="#1E40AF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="0,0,0,4"/>
        <Setter Property="Margin" Value="0,0,0,2"/>
    </Style>

    <Style x:Key="LegacyLabel" TargetType="Label" BasedOn="{StaticResource {x:Type Label}}"/>

    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="LegacyTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}"/>

    <Style TargetType="PasswordBox">
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="LegacyComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}"/>

    <Style TargetType="DatePicker">
        <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="LegacyDatePicker" TargetType="DatePicker" BasedOn="{StaticResource {x:Type DatePicker}}"/>

    <Style x:Key="CardPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Legacy.PanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="18"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Effect" Value="{StaticResource ModernPanelShadow}"/>
    </Style>

    <Style x:Key="InfoBanner" TargetType="Border">
        <Setter Property="Background" Value="#EFF6FF"/>
        <Setter Property="BorderBrush" Value="#DBEAFE"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="14"/>
        <Setter Property="Padding" Value="12,10"/>
    </Style>

    <Style x:Key="WarningBanner" TargetType="Border">
        <Setter Property="Background" Value="#FFF7ED"/>
        <Setter Property="BorderBrush" Value="#FED7AA"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="14"/>
        <Setter Property="Padding" Value="12,10"/>
    </Style>

    <Style x:Key="ErrorBanner" TargetType="Border">
        <Setter Property="Background" Value="#FEF2F2"/>
        <Setter Property="BorderBrush" Value="#FECACA"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="14"/>
        <Setter Property="Padding" Value="12,10"/>
    </Style>

    <Style TargetType="GroupBox">
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource Legacy.PanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource Legacy.HeaderBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="14"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>

    <Style x:Key="LegacyGroupBox" TargetType="GroupBox" BasedOn="{StaticResource {x:Type GroupBox}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="18"
                            Padding="{TemplateBinding Padding}"
                            Effect="{StaticResource ModernPanelShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="{TemplateBinding Header}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource Legacy.HeaderBrush}"
                                       Margin="0,0,0,12"/>
                            <ContentPresenter Grid.Row="1"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="#F8FAFC"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1,0,0,0"/>
        <Setter Property="Padding" Value="10,6"/>
    </Style>

    <Style x:Key="LegacyStatusBar" TargetType="StatusBar" BasedOn="{StaticResource {x:Type StatusBar}}"/>

    <Style x:Key="DialogWindow" TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Legacy.BgBrush}"/>
    </Style>

    <Style x:Key="DialogActionBar" TargetType="DockPanel">
        <Setter Property="LastChildFill" Value="False"/>
        <Setter Property="Margin" Value="0,16,0,0"/>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource Legacy.BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="LegacyDataGridColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#EEF4FB"/>
        <Setter Property="Foreground" Value="#1E3A5F"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="14,0"/>
        <Setter Property="Height" Value="42"/>
        <Setter Property="BorderBrush" Value="#D8E3EF"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <Border Height="2"
                                VerticalAlignment="Bottom"
                                Background="#C5D8EE"
                                Opacity="0.9"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="#E2EDF8"/>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="#E2EDF8"/>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="#E2EDF8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#D0DBE8"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#E9EFF6"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="AlternatingRowBackground" Value="#F7FBFE"/>
        <Setter Property="RowHeight" Value="38"/>
        <Setter Property="ColumnHeaderHeight" Value="42"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="AutoGenerateColumns" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource LegacyDataGridColumnHeader}"/>
        <Setter Property="CellStyle">
            <Setter.Value>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="14,6"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}"
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="RowStyle">
            <Setter.Value>
                <Style TargetType="DataGridRow">
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                    <Setter Property="MinHeight" Value="38"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F0F7FF"/>
                            <Setter Property="BorderBrush" Value="#B7D0EC"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E4F0FC"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Legacy.AccentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Legacy.TextBrush}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#D7EAFB"/>
                        </MultiTrigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LegacyDataGrid" TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}"/>

</ResourceDictionary>
