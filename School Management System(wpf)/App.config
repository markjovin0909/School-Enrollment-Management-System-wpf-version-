<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <appSettings>
    <!-- Active environment: Local, Remote, or Online -->
    <add key="ActiveEnvironment" value="Local" />
    <!-- Enrollment queue SLA visualization thresholds (in hours) -->
    <add key="EnrollmentQueueSlaWarningHours" value="24" />
    <add key="EnrollmentQueueSlaCriticalHours" value="72" />
    <add key="EnrollmentQueueSlaTrackedStatuses" value="PENDING,RESERVED" />
  </appSettings>

  <connectionStrings>
    <!-- MySQL Local Development -->
    <add name="DbLocal" 
         connectionString="server=localhost; uid=root; password=root; database=School_sms" 
         providerName="MySql.Data.MySqlClient" />
    
    <!-- MySQL Remote (Private Network) -->
    <add name="DbRemote" 
         connectionString="server=; uid=; password=; database=" 
         providerName="MySql.Data.MySqlClient" />
    
    <!-- MySQL Online (Cloud) -->
    <add name="DbOnline" 
         connectionString="server=srv1237.hstgr.io; uid=u621755393_cschool; password=Dssc@2026; database=u621755393_CSchoolManagen" 
         providerName="MySql.Data.MySqlClient" />
  </connectionStrings>

  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.7.2" />
  </startup>
</configuration>
