<Window x:Class="School_Management_System.Views.UserActivityHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity History"
        Width="1100"
        Height="650"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblViewing" Grid.Row="0" FontSize="20" FontWeight="Bold" Margin="4,0,4,8"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Label Content="Search" VerticalAlignment="Center"/>
                <TextBox x:Name="txtSearch" Width="320"/>
            </StackPanel>
            <Button x:Name="btnRefresh" Content="Refresh" Width="110" Style="{StaticResource GhostButton}" DockPanel.Dock="Right"/>
        </DockPanel>

        <DataGrid x:Name="gridHistory" Grid.Row="2" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</Window>
