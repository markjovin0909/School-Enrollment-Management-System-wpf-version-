<Window x:Class="School_Management_System.Views.ArchiveCenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:School_Management_System.Controls"
        Title="Archive Center"
        Width="980" Height="620"
        MinWidth="840" MinHeight="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:SectionHeader Grid.Row="0"
                                Title="Archive Center"
                                Subtitle="Review and restore archived records across entities"
                                Margin="0,0,0,10"/>

        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Margin="0,0,0,8" Padding="10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="{StaticResource Icon.Search}" Style="{StaticResource GlyphIcon}" Margin="0,0,8,0"/>
                    <TextBox x:Name="txtSearch" Width="300" Margin="0,0,8,0"/>
                    <CheckBox x:Name="chkShowRestored" Content="Show Restored" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button x:Name="btnRefresh" Content="Refresh" Width="110" Style="{StaticResource GhostButton}"/>
                    <Button x:Name="btnRestore" Content="Restore Selected" Width="140"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="gridRecords" IsReadOnly="True"/>
    </Grid>
</Window>
