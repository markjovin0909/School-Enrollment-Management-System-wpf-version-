<Window x:Class="School_Management_System.Views.TeacherSearchModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Teachers"
        Width="600" Height="500"
        MinWidth="500" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background}"
        Foreground="{DynamicResource Brush.TextPrimary}"
        FontFamily="{DynamicResource FontFamily.Base}"
        FontSize="13"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.xaml"/>
                <ResourceDictionary Source="/Themes/Typography.xaml"/>
                <ResourceDictionary Source="/Themes/Buttons.xaml"/>
                <ResourceDictionary Source="/Themes/Icons.xaml"/>
                <ResourceDictionary Source="/Themes/Inputs.xaml"/>
                <ResourceDictionary Source="/Themes/Cards.xaml"/>
                <ResourceDictionary Source="/Themes/Dialogs.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="14"
            Background="{DynamicResource Brush.Background}"
            BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                <Button x:Name="btnClose"
                        Style="{StaticResource DialogCloseButton}"
                        DockPanel.Dock="Right"/>
                <StackPanel>
                    <TextBlock Text="Teachers" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock Text="Search for a teacher or add a new one" Opacity="0.7" Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>

            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{DynamicResource Brush.SurfaceAlt}"
                        BorderBrush="{DynamicResource Brush.Border}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="8,0"
                        Margin="0,0,10,0">
                    <DockPanel>
                        <TextBlock Text="{StaticResource Icon.Search}"
                                   Style="{StaticResource GlyphIcon}"
                                   Opacity="0.55"
                                   Margin="0,0,8,0"/>
                        <TextBox x:Name="txtSearch"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalAlignment="Center"
                                 FontSize="13"/>
                    </DockPanel>
                </Border>

                <Button x:Name="btnAddTeacher"
                        Grid.Column="1"
                        Content="+ Add Teacher"
                        Width="120"
                        Padding="12,8"/>
            </Grid>

            <Border Grid.Row="2"
                    Background="{DynamicResource Brush.SurfaceAlt}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="10">
                <Grid>
                    <ListBox x:Name="lstTeachers"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Opacity="0.6" FontSize="11">
                                                <Run Text="Employee No: "/><Run Text="{Binding EmployeeNo}"/>
                                                <Run Text="  |  "/><Run Text="{Binding Specialization}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <Path Grid.Column="1"
                                              Width="8"
                                              Height="12"
                                              Stretch="Uniform"
                                              VerticalAlignment="Center"
                                              Stroke="#94A3B8"
                                              StrokeThickness="1.5"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"
                                              StrokeLineJoin="Round"
                                              Data="M0,0 L6,6 L0,12"
                                              Margin="12,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <TextBlock x:Name="txtNoResults"
                               Text="No teachers found matching your search."
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Opacity="0.5"
                               Visibility="Collapsed"/>
                </Grid>
            </Border>

            <TextBlock Grid.Row="3" x:Name="txtFooterInfo" Margin="0,8,0,0" Opacity="0.5" FontSize="11"/>
        </Grid>
    </Border>
</Window>
