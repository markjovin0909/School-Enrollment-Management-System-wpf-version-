<Window x:Class="School_Management_System.Views.SchedulesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:School_Management_System.Controls"
        Title="Schedules"
        Width="1320" Height="780"
        MinWidth="1120" MinHeight="640"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.65*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:SectionHeader Grid.Row="0" Grid.ColumnSpan="2"
                                Title="Schedules"
                                Subtitle="Assign offering, room, and timeslot details per teaching day"
                                Margin="0,0,0,10"/>

        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,8,8" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" x:Name="cboFilterSchoolYear" Margin="0,0,8,0" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                <ComboBox Grid.Column="1" x:Name="cboFilterTeacher" Margin="0,0,8,0" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                <ComboBox Grid.Column="2" x:Name="cboFilterSection" Margin="0,0,8,0" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                <TextBox Grid.Column="3" x:Name="txtSearch" Margin="0,0,8,0"/>
                <Button Grid.Column="4" x:Name="btnRefresh" Content="Refresh" Width="90" Style="{StaticResource GhostButton}"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" Grid.Column="0" x:Name="gridSchedules" IsReadOnly="True" Margin="0,0,8,0"/>

        <ScrollViewer Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Margin="8,0,0,0" VerticalScrollBarVisibility="Auto">
            <Border Style="{StaticResource CardPanel}">
                <StackPanel>
                    <controls:SectionHeader Title="Schedule Details" Subtitle="Control schedule assignment and export workflow" Margin="0,0,0,8"/>
                    <Label Content="Class Offering"/>
                    <ComboBox x:Name="cboOffering" DisplayMemberPath="Label" SelectedValuePath="Id"/>
                    <Label Content="Room" Margin="0,8,0,0"/>
                    <ComboBox x:Name="cboRoom" DisplayMemberPath="Label" SelectedValuePath="Id"/>
                    <Label Content="Time Slot" Margin="0,8,0,0"/>
                    <ComboBox x:Name="cboTimeSlot" DisplayMemberPath="Label" SelectedValuePath="Id"/>
                    <Label Content="Day" Margin="0,8,0,0"/>
                    <ComboBox x:Name="cboDay"/>
                    <Label Content="Start (HH:mm)" Margin="0,8,0,0"/>
                    <TextBox x:Name="txtStart"/>
                    <Label Content="End (HH:mm)" Margin="0,8,0,0"/>
                    <TextBox x:Name="txtEnd"/>

                    <WrapPanel Margin="0,12,0,0">
                        <Button x:Name="btnAdd" Content="Add" Width="90"/>
                        <Button x:Name="btnSave" Content="Save" Width="90"/>
                        <Button x:Name="btnDelete" Content="Delete" Width="90" Style="{StaticResource GhostButton}"/>
                        <Button x:Name="btnClear" Content="Clear" Width="90" Style="{StaticResource GhostButton}"/>
                        <Button x:Name="btnExport" Content="Export" Width="90" Style="{StaticResource GhostButton}"/>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Window>
