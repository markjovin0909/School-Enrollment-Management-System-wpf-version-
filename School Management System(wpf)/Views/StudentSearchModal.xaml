<Window x:Class="School_Management_System.Views.StudentSearchModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Students"
        Width="600" Height="500"
        MinWidth="500" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background}"
        Foreground="{DynamicResource Brush.TextPrimary}"
        FontFamily="{DynamicResource FontFamily.Base}"
        FontSize="13"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.xaml"/>
                <ResourceDictionary Source="/Themes/Typography.xaml"/>
                <ResourceDictionary Source="/Themes/Buttons.xaml"/>
                <ResourceDictionary Source="/Themes/Inputs.xaml"/>
                <ResourceDictionary Source="/Themes/Cards.xaml"/>
                <ResourceDictionary Source="/Themes/Dialogs.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="14"
            Background="{DynamicResource Brush.Background}"
            BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                <Button x:Name="btnClose"
                        Content="&#xE711;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="14"
                        Width="32" Height="32"
                        Style="{StaticResource GhostButton}"
                        DockPanel.Dock="Right"
                        VerticalAlignment="Top"/>
                <StackPanel>
                    <TextBlock Text="Students"
                               FontSize="20"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Search for a student or add a new one"
                               Opacity="0.7"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>

            <!-- Search Bar + Add Button -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{DynamicResource Brush.SurfaceAlt}"
                        BorderBrush="{DynamicResource Brush.Border}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="8,0"
                        Margin="0,0,10,0">
                    <DockPanel>
                        <TextBlock Text="&#xE721;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="14"
                                   VerticalAlignment="Center"
                                   Opacity="0.5"
                                   Margin="0,0,8,0"/>
                        <TextBox x:Name="txtSearch"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalAlignment="Center"
                                 FontSize="13"/>
                    </DockPanel>
                </Border>

                <Button x:Name="btnAddStudent"
                        Grid.Column="1"
                        Content="+ Add Student"
                        Width="120"
                        Padding="12,8"/>
            </Grid>

            <!-- Student List -->
            <Border Grid.Row="2"
                    Background="{DynamicResource Brush.SurfaceAlt}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="10">
                <Grid>
                    <ListBox x:Name="lstStudents"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding FullName}"
                                                       FontWeight="SemiBold"
                                                       FontSize="13"/>
                                            <TextBlock Opacity="0.6" FontSize="11">
                                                <Run Text="LRN: "/><Run Text="{Binding Lrn}"/>
                                                <Run Text="  |  Student No: "/><Run Text="{Binding StudentNumber}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1"
                                                   Text="&#xE76C;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   VerticalAlignment="Center"
                                                   Opacity="0.4"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- No Results Message -->
                    <TextBlock x:Name="txtNoResults"
                               Text="No students found matching your search."
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Opacity="0.5"
                               Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- Footer info -->
            <TextBlock Grid.Row="3"
                       x:Name="txtFooterInfo"
                       Margin="0,8,0,0"
                       Opacity="0.5"
                       FontSize="11"/>
        </Grid>
    </Border>
</Window>
