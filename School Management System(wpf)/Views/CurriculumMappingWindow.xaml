<Window x:Class="School_Management_System.Views.CurriculumMappingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Curriculum Mapping"
        Style="{StaticResource DialogWindow}"
        Width="640"
        Height="460"
        MinWidth="640"
        MinHeight="460"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background}"
        Foreground="{DynamicResource Brush.TextPrimary}"
        FontFamily="{DynamicResource FontFamily.Base}"
        FontSize="13"
        ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.xaml"/>
                <ResourceDictionary Source="/Themes/Typography.xaml"/>
                <ResourceDictionary Source="/Themes/Buttons.xaml"/>
                <ResourceDictionary Source="/Themes/Inputs.xaml"/>
                <ResourceDictionary Source="/Themes/Cards.xaml"/>
                <ResourceDictionary Source="/Themes/Dialogs.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Style="{StaticResource DialogRootGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource DialogShell}">
            <StackPanel>
                <TextBlock x:Name="txtDialogTitle" Text="Add Subject Mapping" Style="{StaticResource DialogHeaderTitle}"/>
                <TextBlock x:Name="txtDialogSubtitle"
                           Text="Assign a subject to a curriculum, grade level, and semester in one modal flow."
                           Style="{StaticResource DialogHeaderSubtitle}"/>
            </StackPanel>
        </Border>

        <Border x:Name="validationSummaryHost"
                Grid.Row="1"
                Style="{StaticResource ErrorBanner}"
                Visibility="Collapsed"
                Margin="0,0,0,12">
            <TextBlock x:Name="txtValidationSummary"
                       Foreground="{StaticResource Brush.Danger}"
                       TextWrapping="Wrap"/>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource DialogSection}">
            <StackPanel>
                <TextBlock Text="Mapping Details" Style="{StaticResource Text.CaptionStrong}" Margin="0,0,0,8"/>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Curriculum"/>
                    <TextBox Grid.Row="1" Grid.ColumnSpan="2" x:Name="txtCurriculumName" IsReadOnly="True" Margin="0,0,0,12"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Grade Level"/>
                    <Label Grid.Row="2" Grid.Column="1" Content="Subject"/>
                    <ComboBox Grid.Row="3" Grid.Column="0" x:Name="cboMapGrade" Margin="0,0,8,12" DisplayMemberPath="Code" SelectedValuePath="Id"/>
                    <ComboBox Grid.Row="3" Grid.Column="1" x:Name="cboMapSubject" Margin="8,0,0,12" DisplayMemberPath="Label" SelectedValuePath="Id"/>

                    <Label Grid.Row="4" Grid.Column="0" Content="Semester"/>
                    <Label Grid.Row="4" Grid.Column="1" Content="Required"/>
                    <ComboBox Grid.Row="5" Grid.Column="0" x:Name="cboMapSemester" Margin="0,0,8,0"/>
                    <ComboBox Grid.Row="5" Grid.Column="1" x:Name="cboMapRequired" Margin="8,0,0,0"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Sort Order"/>
                    <TextBox Grid.Column="1" x:Name="txtMapSort"/>
                </Grid>
            </StackPanel>
        </Border>

        <DockPanel Grid.Row="3" Style="{StaticResource DialogActionBar}" LastChildFill="False">
            <Button x:Name="btnCancel" Content="Cancel" Width="110" Style="{StaticResource SecondaryActionButton}" DockPanel.Dock="Right" Margin="8,0,0,0"/>
            <Button x:Name="btnSave" Content="Create Mapping" Width="132" DockPanel.Dock="Right" Margin="8,0,0,0"/>
        </DockPanel>
    </Grid>
</Window>
